# The only operator-managed local configuration file.
# scripts/install-master.sh creates cluster.local.env from this template.
# The real file is Git-ignored and kept mode 600.

# Installer replaces this with cluster1 on Ubuntu master / cluster2 on CentOS master.
CLUSTER=AUTODETECT

# Private management network only.
# Format: name@IPv4,name@IPv4,...
# Examples:
#   Cluster 1: kfai-cpu-01@192.168.100.11,kfai-cpu-02@192.168.100.12
#   Cluster 2: kfai-cpu-10@172.20.1.4,kfai-cpu-11@172.20.1.5
NODES=EDIT_ME

# FastAPI Basic Auth + initial Grafana admin login.
ADMIN_USERNAME=clusteradmin
# Leave blank; first install generates and writes a random password here.
ADMIN_PASSWORD=

# Normally leave these defaults unchanged. Both UIs bind to master loopback.
UI_PORT=8080
GRAFANA_PORT=3000
